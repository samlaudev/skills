# Engineering

Skills for code work.

## User-invoked

Reachable only when you type them (Claude Code: `disable-model-invocation: true`; Codex: `policy.allow_implicit_invocation: false` in `agents/openai.yaml`).

- **[architect-from-spec](./architect-from-spec/SKILL.md)**: Turn an approved spec into an implementation-ready codebase architecture: modules, seams, dependency rules, project structure, coding and testing rules, then scaffold and verify the skeleton.

## Model-invoked

Model- or user-reachable (rich trigger phrasing so the model can reach for them).

_None yet._
